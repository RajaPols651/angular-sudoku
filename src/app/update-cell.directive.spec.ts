import { UpdateCellDirective } from './update-cell.directive';

describe('UpdateCellDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdateCellDirective();
    expect(directive).toBeTruthy();
  });
});
